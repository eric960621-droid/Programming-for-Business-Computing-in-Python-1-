題目 1：總共多少錢題目敘述你手上有面額 50、10、5、1 元的銅板各 $w$、$x$、$y$、$z$ 個，請計算你一共有多少錢。輸入輸出格式輸入：在每筆測試資料中會有四行，每行含有一個整數，依序是 $w$、$x$、$y$、$z$。任意一個數字都會介於 0 到 10 之間（包含 0 跟 10）。輸出：讀入四個數字之後，請依照題目規則輸出一個整數代表總計金額。範例測試輸入：Plaintext1
1
0
1
輸出：Plaintext61
Python 參考實作Pythonimport sys

def solve():
    try:
        # 依序讀入 w, x, y, z
        w = int(input())
        x = int(input())
        y = int(input())
        z = int(input())
        
        # 計算總金額：50w + 10x + 5y + 1z
        total = (w * 50) + (x * 10) + (y * 5) + (z * 1)
        
        print(total)
    except EOFError:
        pass

if __name__ == "__main__":
    solve()
解題思路這是一題基礎的算術運算題，重點在於：標準輸入讀取：題目要求分四行輸入，因此需要連續呼叫四次 input()。權重計算：將每個變數乘以其對應的貨幣面額並加總即可。複雜度分析：時間複雜度：$O(1)$，僅進行固定次數的常數運算。空間複雜度：$O(1)$，僅使用極少量變數儲存輸入值。
