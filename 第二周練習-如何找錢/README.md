# 該怎麼找錢 (How to Give Change)

## 📝 題目敘述
如果你在一家零售店幫消費的客人結帳，你可能需要快速地挑出合適且數量正確的鈔票與零錢。假設客人的消費金額 $a$ 一定是 $1$ 到 $1000$ 之間的整數，而你有無限量的 $500$、$100$、$50$、$10$、$5$、$1$ 這些面額的鈔票和零錢，我們希望你能依照下面的規則找錢：

1. 你找錢的總額要是 $1000 - a$。
2. 與其給客人五張 $100$ 元，不如給他一張 $500$ 元；與其給客人兩個 $50$ 元，不如給他一張 $100$ 元......依此類推。

在本題中，你將會被給予上述的整數 $a$，而你要找出符合上述規則的唯一找錢方式。

---

## 📥 輸入輸出格式

### 輸入格式
在每筆測試資料中，會有一個整數 $a$ 代表客人的消費金額，$a$ 會介於 $1$ 到 $1000$ 之間（包含 $1$ 跟 $1000$）。

### 輸出格式
讀入 $a$ 之後，依依照題目指定的規則找出每種面額的鈔票或鋼板應該要給幾張或幾個，然後在一行中依序輸出**六個整數**，分別代表面額由大到小的鈔票或零錢所需的張數或個數。
*   面額順序：`500`, `100`, `50`, `10`, `5`, `1`
*   如果某種面額不需要，輸出 `0`。
*   兩個整數之間用一個空白字元隔開，最後一個數字後面沒有空格。

---

## 💡 範例說明

| 輸入金額 ($a$) | 找錢總額 ($1000-a$) | 輸出結果 | 說明 |
| :--- | :--- | :--- | :--- |
| `200` | `800` | `1 3 0 0 0 0` | 1張 500 元和 3張 100 元 |
| `286` | `714` | `1 2 0 1 0 4` | 1張 500、2張 100、1個 10 和 4個 1 |
| `925` | `75` | `0 0 1 2 1 0` | 1個 50、2個 10 和 1個 5 |

---

## 🧠 解題思路
本題適合使用 **貪婪演算法 (Greedy Algorithm)**：
1. 先計算目標找錢金額 `change = 1000 - a`。
2. 建立面額清單：`[500, 100, 50, 10, 5, 1]`。
3. 由大到小遍歷面額，利用整除運算（`//`）取得該面額數量，再利用取餘數運算（`%`）更新剩餘餘額。

---

## 📊 複雜度分析

| 維度 | 複雜度 | 說明 |
| :--- | :--- | :--- |
| **時間複雜度** | $O(1)$ | 固定的 6 種面額，運算次數固定。 |
| **空間複雜度** | $O(1)$ | 僅需儲存固定的變數與列表。 |
